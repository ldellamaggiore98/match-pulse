{
  "name": "matchpulse",
  "private": true,
  "workspaces": ["backend", "scraper"],
  "scripts": {
    "start": "sh start.sh",
    "postinstall": "cd backend && npx prisma generate && npx prisma migrate deploy"
  }
}
