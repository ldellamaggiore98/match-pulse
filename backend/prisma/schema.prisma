generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Standing {
  id        Int      @id @default(autoincrement())
  team      String
  points    String
  group     String
  createdAt DateTime @default(now())
}
